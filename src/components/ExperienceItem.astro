---
const { title, company, startDate, endDate, description } = Astro.props;
---
 
<div data-exp={company} class="border border-gradient p-4 my-16 rounded-xl experiencia bg-gray-800 hidden" >
    <div class="min-h-[240px] flex flex-col justify-between">
        <div class="flex flex-col gap-2">
            <h3 class="text-xl font-semibold text-primary">{title}</h3>
            <h4 class="text-xl">{company}</h4>
            <time class="text-sm text-accent">
                {startDate} a {endDate}
            </time>
            <p>{description}</p>
        </div>
        <hr class="mt-4 border-gray-300" />
    </div>
</div>