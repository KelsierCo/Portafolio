---
const { company, title, startDate, endDate, description } = Astro.props;
---

<div class="border border-gray-700 p-4 rounded-xl experiencia bg-gray-300">
    <h3 class="text-lg font-semibold text-secondary">{title}</h3>
    <h4 class="text-md">{company}</h4>
    <time class="text-sm text-gray-500">
        {startDate} a {endDate}
    </time>
    <p class="text-black mt-2">{description}</p>
    <hr class="my-4 border-gray-700" />
</div>