---
const { title, company, startDate, endDate, description } = Astro.props;
---
 
<div class="border border-gray-700 p-4 my-16 rounded-xl experiencia bg-gray-300 min-h-[240px] flex flex-col justify-between">
    <div>
        <h3 class="text-lg font-semibold text-secondary">{title}</h3>
        <h4 class="text-md">{company}</h4>
        <time class="text-sm text-gray-500">
            {startDate} a {endDate}
        </time>
        <p class="text-black mt-2">{description}</p>
    </div>
    <hr class="mt-4 border-gray-700" />
</div>