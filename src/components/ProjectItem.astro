---
const { title, image, description, link } = Astro.props;
---


<li class="mb-6 bg-gray-600 p-4 rounded-lg shadow-md">
    <div class="flex flex-col md:flex-row items-center gap-4">
        <img src={image} alt={title} class="w-full md:w-1/3 rounded-lg shadow-md" />
        <div class="md:w-2/3">
            <h2 class="text-xl font-semibold text-primary">{title}</h2>
            <p class="text-white">{description}</p>
            <a href={link} target="_blank" rel="noopener noreferrer" class="text-blue-500 hover:underline">Ver proyecto</a>
        </div>
    </div>
</li>