---
const { title, image, description, link } = Astro.props;
---


<article class="bg-neutral p-4 rounded-lg">
    <div class="flex flex-col md:flex-row items-center gap-4">
        <img src={image} alt={title} class="w-full md:w-1/3 rounded-lg border-2 border-black transition duration-500 ease-in-out hover:scale-105" />
        <div class="md:w-2/3">
            <h2 class="text-xl font-semibold text-primary">{title}</h2>
            <p class="text-whie">{description}</p>
            <a href={link} target="_blank" rel="noopener noreferrer" class="text-accent hover:underline">Ver proyecto</a>
        </div>
    </div>
</article>