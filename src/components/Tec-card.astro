---
const { image, title, description, link } = Astro.props;
---


<div class="tec-card">
    <img src={image} alt={title} class="tec-image" />
    <h3 class="tec-title">{title}</h3>
    <p class="tec-description">{description}</p>
    <a href={link} class="tec-link">Learn More</a>
</div>
