---
const { title, image } = Astro.props;
---

<div class="w-[150px] h-[150px] max-w-[150px] max-h-[300px] flex flex-col items-center justify-center shadow-md rounded-xl bg-neutral-900 text-white p-2 overflow-hidden">
  <img src={image} alt={`Imagen de ${title}`} class="w-20 h-20 object-contain mb-2" />
  <h3 class="text-base font-bold text-center m-0">{title}</h3>
</div>
