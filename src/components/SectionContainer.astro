---
const { id } = Astro.props;
---

<section
    id={id},
    data-section={id}
    class={`section min-h-screen max-h-screen scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl sm:max-w-xl`}
>
    <slot />
</section>