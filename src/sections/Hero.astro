---
import { Image } from 'astro:assets';
import fotoCarlos from '@assets/images/foto_carlos.webp';
import { SOCIAL } from '@/assets/const/social';
---

<div class="flex items-center min-h-screen gap-16">
	<div>
		<Image src={fotoCarlos} alt="Imagen Carlos Moreno" class="rounded-full border-4 border-gray-300" />
	</div>
	<div class="flex flex-col gap-6">
		<h1 class="text-4xl text-primary font-bold">¡Hola, soy Carlos Moreno!</h1>
		<p class="[&>strong]:text-accent [&>strong]:font-semibold">1 año de experiencia. Tecnologo en sistematizacion de datos, <strong>especializado en web Fullstack y Backend</strong></p>
		<div class="flex justify-between items-center">
			<div class="flex gap-4 mt-4">
				{
					SOCIAL.map(({ name, url, image }) => (
						<a 
							href={url}
							target="_blank"
							rel="noopener noreferrer"
							class="text-white transition-colors"
						>
							<image.logo alt={`${name} logo`} />
						</a>
					))
				}
			</div>
			<div class="text-right mt-4">
				<p>🌐 Bogotá, Colombia</p>
			</div>
		</div>
	</div>
</div>